# Temporary isolated Android build workspace

This public repository is used only for a sanitized, unsigned Flutter build. No private signing key, Drive URL, account credential, or company document identifier is stored here.
